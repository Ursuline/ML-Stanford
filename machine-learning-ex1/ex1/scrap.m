clear ; close all; clcdata = load('ex1data1.txt');X = data(:, 1); y = data(:, 2);m = length(y); % number of training examplesX = [ones(m, 1), data(:,1)]; % Add a column of ones to xtheta = zeros(2, 1); % initialize fitting parametersXyX(1,1)X(1,2)X(3,:)%J = 1.0 / (2.0 * m) * (theta' * X' - y)^2;%{temp  = 0;for i = 1:m  %temp = temp + (theta(1) * X(i, 1) + theta(2) * X(i, 2) - y(i))^2;  temp = temp + (theta'*X(i,:)' - y(i))^2;endforJ = 1.0 / (2.0 * m) * temp;%}%Temp  = theta - alpha/m * (theta' * X' - y) * X;    %d + (theta(1)*X(i, 1) + theta(2) * X(i, 2) - y(i))^2;